export const periodOptions = {
  month: 'month',
  week: 'week',
  default: 'week'
};

export const periodDisplayOptions = {
  month: 'Month',
  week: 'Week'
};

const dayHours = 24;
const hourMins = 60;
const minSecs = 60;
const secMs = 1000;
export const msPerDay = dayHours * hourMins * minSecs * secMs;

export const weekDays = 7;
export const monthDays = 30;
